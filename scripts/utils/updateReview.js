import { supabase } from "../api/database";
document.getElementById('').addEventListener('click',async () =>
{
const {error} =await supabase.from('prductReview').update({

})}
)
function checkEmptyFields(){
    
}